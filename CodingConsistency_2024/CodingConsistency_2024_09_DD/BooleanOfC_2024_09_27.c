#include <stdio.h>
#include <stdbool.h>
// 0 is false and any other number than 0 is True
// Doesn't have built in boolean values? Must import first.
int main(){
    bool BooleanValue = true;
    printf("%d",BooleanValue);
    return 0;
}